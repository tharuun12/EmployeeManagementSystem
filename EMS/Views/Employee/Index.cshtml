@model EMS.ViewModels.EmployeeProfileViewModel

@{
    ViewData["Title"] = "My Profile";
}

<div class="employee-profile-container">
    <div class="employee-profile-card">
        <h2 class="profile-title">Employee Profile</h2>

        <div class="profile-info">
            <div class="profile-row">
                <span class="label">Full Name:</span>
                <span class="value">@Model.Employee.FullName</span>
            </div>
            <div class="profile-row">
                <span class="label">Email:</span>
                <span class="value">@Model.Employee.Email</span>
            </div>
            <div class="profile-row">
                <span class="label">Phone:</span>
                <span class="value">@Model.Employee.PhoneNumber</span>
            </div>
            <div class="profile-row">
                <span class="label">Role:</span>
                <span class="value">@Model.Employee.Role</span>
            </div>
            <div class="profile-row">
                <span class="label">Department:</span>
                <span class="value">@Model.Employee.Department?.DepartmentName</span>
            </div>
            <div class="profile-row">
                <span class="label">Manager:</span>
                <span class="value">@Model.ManagerName</span>
            </div>
            <div class="profile-row">
                <span class="label">Leave Balance:</span>
                <span class="value">@Model.Employee.LeaveBalance</span>
            </div>
            <div class="profile-row">
                <span class="label">Status:</span>
                <span class="value">@(Model.Employee.IsActive ? "Active" : "Inactive")</span>
            </div>
        </div>
    </div>
</div>


@* @if (Model?.Employee != null)
{
    <div class="card p-4 shadow rounded">
        <dl class="row">
            <dt class="col-sm-3 font-weight-bold">Full Name</dt>
            <dd class="col-sm-9">@Model.Employee.FullName</dd>

            <dt class="col-sm-3 font-weight-bold">Email</dt>
            <dd class="col-sm-9">@Model.Employee.Email</dd>

            <dt class="col-sm-3 font-weight-bold">Phone</dt>
            <dd class="col-sm-9">@Model.Employee.PhoneNumber</dd>

            <dt class="col-sm-3 font-weight-bold">Role</dt>
            <dd class="col-sm-9">@Model.Employee.Role</dd>

            <dt class="col-sm-3 font-weight-bold">Department</dt>
            <dd class="col-sm-9">@Model.Employee.Department?.DepartmentName ?? "N/A"</dd>

            <dt class="col-sm-3 font-weight-bold">Manager</dt>
            <dd class="col-sm-9">@Model.ManagerName</dd>

            <dt class="col-sm-3 font-weight-bold">Leave Balance</dt>
            <dd class="col-sm-9">@Model.Employee.LeaveBalance</dd>

            <dt class="col-sm-3 font-weight-bold">Status</dt>
            <dd class="col-sm-9">@(Model.Employee.IsActive ? "Active" : "Inactive")</dd>
        </dl>
    </div>
}
else
{
    <div class="alert alert-warning">
        No employee data found for the logged-in user.
    </div>
} *@